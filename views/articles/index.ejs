<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog</title>
</head>

<body>
  <div class="container">
    <h1 class="mb-4">Blog Articles</h1>
    <a href="/articles/new" class="btn btn-success">New Article</a>
    <% articles.forEach(ele=> { %>
      <div class="card mt-4">
        <div class="card-body">
          <h4 class="card-titlr">
            <%= ele.title %>
          </h4>
          <div class="card-subtitle text-muted mb-2">
            <%= ele.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text mb-2">
            <%= ele.description %>
          </div>
          <a href="articles/<%= ele._id %>" class="btn btn-primary">Read More</a>
          <a href="/articles/edit/<%= ele._id %>" class="btn btn-info">Edit</a>
          <form style="display: inline;" action="/articles/<%= ele._id%>?_method=DELETE" method="post">
            <input type="submit" value="Delete" class="btn btn-danger">
          </form>
        </div>
      </div>
      <% }) %>
  </div>
</body>

</html>